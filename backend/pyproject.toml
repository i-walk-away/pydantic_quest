[tool.ruff]
line-length = 100
target-version = "py312"
src = ["src"]
exclude = ["migrations", "scripts"]

[tool.ruff.lint]
select = ["E", "F", "B", "UP", "SIM", "C4", "T20", "ARG"]
ignore = ["E501", "B008"]

[tool.ruff.format]
